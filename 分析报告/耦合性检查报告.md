# Mind-OS Coupling Analysis Report

## 1. Current Coupling Points (The "Hard Links")

### 📂 Directory Hardcoding
- **File**: `scripts/consistency_check.py`
- **Issue**: The `directories` list is hardcoded: `['知识画像', '心理画像', '量化算法', '深度觉察']`.
- **Impact**: If a user renames "知识画像" to "Knowledge_Base", the auditing engine will silently ignore it.

### 🖼️ Output Asset Path
- **File**: `scripts/radar_gen.py`
- **Issue**: The output path `分析报告/latest_radar.png` is hardcoded.
- **Impact**: Requires the manual creation of the "分析报告" folder and breaks if the user wants to store reports elsewhere.

### 📊 Data Logic Coupling
- **File**: `scripts/radar_gen.py`
- **Issue**: Labels like `['Cognition', 'Execution', ...]` are internal to the script.
- **Impact**: If the user changes the "五维雷达" definition in `量化算法/算法规则.md`, the chart generator will still show the old labels.

### 🧩 Frontmatter Dependency
- **File**: `scripts/consistency_check.py`
- **Issue**: Strict check for `content.startswith('---')`.
- **Impact**: Doesn't allow for BOM or accidental whitespace at the start of files.

---

## 2. Decoupling Strategy (The "Elastic Plan")

### 🛠️ Introducing `config.yaml`
Centralize all environment settings (paths, folder names, dimension labels) into a single configuration file.

### 🔍 Dynamic Folder Discovery
Modify scripts to scan for folders containing a `.mind-os-root` marker or use the config file to map directory purposes.

### 🛡️ Defensive Parsing
Upgrade `check_metadata` to use regex for finding YAML blocks, making it resilient to minor formatting issues.

### 🔗 Interface Standardization
Ensure `mind-os.py` acts as a clean bridge, passing configuration objects to scripts rather than having scripts manage their own paths.
